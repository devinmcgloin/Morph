
<!DOCTYPE html>
<html>
   <head>
      <meta charset="utf-8">
      <link rel="stylesheet" href="/assets/css/normalize.css">
      <link rel="stylesheet" href="/assets/css/main.css">

   </head>
   <body>
      <div class="container">
        {{ template "logo" }}

         <h2>{{.Title}}</h2>
         <p>{{.Desc}}</p>
         <p>{{.PublishTime.Format "2006 Jan 02" }}</p>
         <!-- <img class="photo-feature"> -->
         <p>f/{{.Aperture}} 1/{{.ExposureTime}} sec {{.FocalLength}}mm ISO {{.ISO}}</p>

      </div>
   </body>
</html>
